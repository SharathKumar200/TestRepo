package com.JavaEnum;

/*
 * public enum Day { SUNDAY("Weekend"), MONDAY("Weekday"), TUESDAY("Weekday"),
 * WEDNESDAY("Weekday"), THURSDAY("Weekday"), FRIDAY("Weekday"),
 * SATURDAY("Weekend");
 * 
 * private String type;
 * 
 * // Constructor Day(String type) { this.type = type; }
 * 
 * // Method to get the type of the day public String getType() { return type; }
 * 
 * // Method to check if the day is a weekend public boolean isWeekend() {
 * return this == SUNDAY || this == SATURDAY; } }
 */
/*
 * public enum Day { SUNDAY("Weekend"), MONDAY("Weekday"), TUESDAY("Weekday"),
 * WEDNESDAY("Weekday"), THURSDAY("Weekday"), FRIDAY("Weekday"),
 * SATURDAY("Weekend");
 * 
 * private String type;
 * 
 * // Constructor to initialize the 'type' field Day(String type) { this.type =
 * type; }
 * 
 * // Method to get the type of the day public String getType() { return type; }
 * 
 * // Method to check if the day is a weekend public boolean isWeekend() {
 * return this == SUNDAY || this == SATURDAY; } }
 * 
 * public class EnumDay { public static void main(String[] args) { // Iterate
 * through all enum constants for (Day day : Day.values()) { // Print each day
 * with its type and whether it's a weekend System.out.println(day + " is a " +
 * day.getType() + " and it is " + (day.isWeekend() ? "a weekend." :
 * "not a weekend.")); } } }
 */
